<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SQLDictMapper">

	<resultMap type="comDict" id="comDictResultMap">
		<result column="dictName" property="dictName"/>
		<result column="dictValue" property="dictValue"/>
	</resultMap>
	
	
	<!-- ****************************custom * start*********************************** -->
		<!-- 后台生效用户 -->
		<select id="bg_userEffective" parameterType="String" resultMap="comDictResultMap">
			select userName as dictName, userId as dictValue
			from bg_user_info 
			where effective = '01'
			<if test="value!=null and value!=''">
				and userId= #{value} 
			</if>
		</select>
		
		<!-- 后台生效角色 -->
		<select id="bg_roleEffective" parameterType="String" resultMap="comDictResultMap">
			select roleName as dictName, roleId as dictValue
			from bg_role_info 
			where effective = '01'
			<if test="value!=null and value!=''">
				and roleId= #{value} 
			</if>
		</select>
		
		<!-- 后台生效菜单 -->
		<select id="bg_menuEffective" parameterType="String" resultMap="comDictResultMap">
			select menuName as dictName, menuId as dictValue
			from bg_menu_tree
			where effective = '01'
			<if test="value!=null and value!=''">
				and menuId= #{value} 
			</if>
		</select>
		
		<!-- 生效代码生成器 -->
		<select id="bg_mapleEffective" parameterType="String" resultMap="comDictResultMap">
			select mapleName as dictName, mapleId as dictValue
			from bg_maple_main
			where effective = '01'
			<if test="value!=null and value!=''">
				and mapleId= #{value} 
			</if>
		</select>
		
		<!-- com_dictEffective -->
		<select id="com_dictEffective" parameterType="String" resultMap="comDictResultMap">
			select dictName as dictName, dictId as dictValue
			from com_dict_tree
			where effective = '01'
			<if test="value!=null and value!=''">
				and dictId = #{value} 
			</if>
		</select>
		
		<!-- 生效平台用户 -->
		<select id="com_appUserEffective" parameterType="String" resultMap="comDictResultMap">
			select appUserName as dictName, appUserId as dictValue
			from com_app_user_tree
			where effective = '01'
			<if test="value!=null and value!=''">
				and appUserId = #{value} 
			</if>
		</select>
		
		<!-- 生效平台用户（4S店） -->
		<select id="com_appUserEffective4S" parameterType="String" resultMap="comDictResultMap">
			select appUserName as dictName, appUserId as dictValue
			from com_app_user_tree
			where effective = '01'
				and roleId = '03'
			<if test="value!=null and value!=''">
				and appUserId = #{value} 
			</if>
		</select>
		
		<!-- 生效零配件 -->
		<select id="com_sparepartEffective" parameterType="String" resultMap="comDictResultMap">
			select sparepartName as dictName, sparepartId as dictValue
			from com_sparepart_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and sparepartId = #{value} 
			</if>
		</select>
		
		<!-- 生效供应商 -->
		<select id="com_supplierEffective" parameterType="String" resultMap="comDictResultMap">
			select supplierName as dictName, supplierId as dictValue
			from com_supplier_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and supplierId = #{value} 
			</if>
		</select>
		
		<!-- 生效商品类别 -->
		<select id="com_productTypeEffective" parameterType="String" resultMap="comDictResultMap">
			select productCategoryName as dictName, productCategoryId as dictValue
			from com_product_category_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and productCategoryId = #{value} 
			</if>
				and productCategoryType = '01'
		</select>
		
		<!-- 生效商品模型 -->
		<select id="com_productModelEffective" parameterType="String" resultMap="comDictResultMap">
			select productCategoryName as dictName, productCategoryId as dictValue
			from com_product_category_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and productCategoryId = #{value} 
			</if>
				and productCategoryType = '02'
		</select>
		
		<!-- 生效商品自定义类型 -->
		<select id="com_productCustomTypeEffective" parameterType="String" resultMap="comDictResultMap">
			select productCategoryName as dictName, productCategoryId as dictValue
			from com_product_category_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and productCategoryId = #{value} 
			</if>
				and productCategoryType = '03'
		</select>
		
		<!-- 生效商品 -->
		<select id="com_productEffective" parameterType="String" resultMap="comDictResultMap">
			select productName as dictName, productId as dictValue
			from com_product_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and productId = #{value} 
			</if>
		</select>
		
		<!-- 生效商品规格分类 -->
		<select id="com_styleCategoryEffective" parameterType="String" resultMap="comDictResultMap">
			select styleCategoryName as dictName, styleCategoryId as dictValue
			from com_style_category_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and styleCategoryId = #{value} 
			</if>
		</select>
		
		<!-- 生效商品规格 -->
		<select id="com_productStyleEffective" parameterType="String" resultMap="comDictResultMap">
			select productStyleName as dictName, productStyleId as dictValue
			from com_product_style_info
			where effective = '01'
			<if test="value!=null and value!=''">
				and productStyleId = #{value} 
			</if>
		</select>
		
		
		
	<!-- ****************************common * end  ********************************** -->
	
</mapper>